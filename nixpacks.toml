[phases.setup]
nixPkgs = ["python311", "zlib", "libjpeg", "zbar"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = [
  "mkdir -p uploads data"
]

[start]
cmd = "uvicorn backend.web:app --host 0.0.0.0 --port $PORT --proxy-headers --forwarded-allow-ips='*'"
